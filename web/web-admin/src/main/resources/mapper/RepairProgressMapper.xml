<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.RepairProgressMapper">

    <select id="listVoByRequestId" resultType="com.atguigu.lease.web.admin.vo.repair.RepairProgressVo">
        SELECT
            rp.status,
            rp.content,
            rp.operator_role,
            su.name AS operator_name,
            rp.create_time
        FROM repair_progress rp
                 LEFT JOIN system_user su ON rp.operator_id = su.id AND su.is_deleted = 0
        WHERE rp.is_deleted = 0
          AND rp.request_id = #{requestId}
        ORDER BY rp.create_time ASC
    </select>
</mapper>
